# Customer Purchase Prediction - Environment Configuration

# Project paths (optional - will use defaults if not set)
# PROJECT_ROOT=/path/to/customer-purchase-prediction-ml
# DATA_DIR=./data
# MODELS_DIR=./models

# Model training configuration
RANDOM_SEED=42
TRAIN_VAL_TEST_SPLIT=0.7,0.15,0.15

# Feature engineering settings
SCALING_METHOD=minmax  # options: minmax, standard
ENGINEER_FEATURES=true

# Model hyperparameters (optional - uses defaults if not set)
# LOGISTIC_REGRESSION_C=1.0
# RANDOM_FOREST_N_ESTIMATORS=100
# RANDOM_FOREST_MAX_DEPTH=10
# XGBOOST_N_ESTIMATORS=100
# XGBOOST_MAX_DEPTH=6
# XGBOOST_LEARNING_RATE=0.1

# Evaluation settings
MODEL_SELECTION_CRITERIA=f1_score  # options: f1_score, precision, recall, accuracy

# Logging configuration
LOG_LEVEL=INFO  # options: DEBUG, INFO, WARNING, ERROR

# Data processing settings
BATCH_SIZE=1000  # for large datasets
MEMORY_LIMIT=8GB  # memory usage limit